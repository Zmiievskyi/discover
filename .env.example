# =============================================================================
# OPENAI API CONFIGURATION
# =============================================================================
# Get your API key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional: OpenAI Organization ID (if you have one)
# OPENAI_ORGANIZATION=org-your-organization-id


# =============================================================================
# CRAWLER CONFIGURATION
# =============================================================================
# Target website to crawl
BASE_URL=https://wiki.gcore.lu/

# Maximum number of pages to crawl
MAX_PAGES=50

# Base delay between requests in seconds (becomes 2-6 seconds in stealth mode)
DELAY=2

# Enable stealth mode (random delays, realistic browser headers)
# Values: true or false
STEALTH_MODE=true


# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# SQLite database path (optional, for traditional SQL storage)
DATABASE_PATH=crawl_data.db

# JSON output file path
OUTPUT_FILE=crawl_results.json


# =============================================================================
# VECTOR STORE CONFIGURATION (ChromaDB + OpenAI)
# =============================================================================
# Enable/disable vector store for semantic search
# Values: true or false
VECTOR_STORE_ENABLED=false

# ChromaDB storage directory
VECTOR_STORE_PATH=./chroma_db

# ChromaDB collection name
VECTOR_COLLECTION_NAME=crawled_pages

# OpenAI embedding model
# Options:
# - text-embedding-3-small (1536-dim, $0.02/1M tokens, fast and cheap)
# - text-embedding-3-large (3072-dim, $0.13/1M tokens, better quality)
# - text-embedding-ada-002 (1536-dim, $0.10/1M tokens, legacy)
OPENAI_EMBEDDING_MODEL=text-embedding-3-small


# =============================================================================
# AUTHENTICATION CONFIGURATION
# =============================================================================
# Authentication type: none, cookies, auto_cookies, basic, headers
AUTH_TYPE=cookies

# --- Method 1: Manual Cookies (AUTH_TYPE=cookies) ---
# Provide cookies manually (they won't refresh automatically)
# Format: KEY1=VALUE1;KEY2=VALUE2
# Example: JSESSIONID=abc123;token=xyz789
AUTH_COOKIES=JSESSIONID=71F572ACA7F168D486482260A4E7C0E0;croowd.token_key=_0wiPRNuFIY5vZU7Z1oKngAAAAAAAIACYW50b24uem1paWV2c2t5aUBnY29yZS5sdQ

# --- Method 2: Auto-Refresh Cookies (AUTH_TYPE=auto_cookies) ---
# Automatically refresh cookies when they expire using username/password
# Uncomment these lines to enable auto-refresh:
# AUTH_TYPE=auto_cookies
# AUTH_LOGIN_URL=https://your-site.com/login
# AUTH_USERNAME=your_username
# AUTH_PASSWORD=your_password
# AUTH_LOGIN_USERNAME_FIELD=username  # Optional: default is "username"
# AUTH_LOGIN_PASSWORD_FIELD=password  # Optional: default is "password"
# AUTH_COOKIES=JSESSIONID=initial_cookie  # Optional: use existing cookies first

# --- Method 3: Basic Auth (AUTH_TYPE=basic) ---
# HTTP Basic Authentication with username/password
# AUTH_TYPE=basic
# AUTH_USERNAME=your_username
# AUTH_PASSWORD=your_password

# --- Method 4: Bearer Token (AUTH_TYPE=headers) ---
# Token-based authentication (e.g., JWT)
# AUTH_TYPE=headers
# AUTH_BEARER_TOKEN=your_bearer_token_here


# =============================================================================
# OPTIONAL: TAVILY API (for crawl.py)
# =============================================================================
# TAVILY_API_KEY=tvly-your-tavily-key-here
